import { defineConfig } from 'astro/config';
export default defineConfig({ site: 'https://incandescent-scone-d4ee7d.netlify.app', output: 'static', trailingSlash: 'always', devToolbar: { enabled: false } });
